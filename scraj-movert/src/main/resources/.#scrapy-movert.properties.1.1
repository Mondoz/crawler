# firefox binary path
firefox.path = D\:\\soft\\Mozilla Firefox\\firefox.exe

# os
os = windows

# parser
content.title.min.length = 4
content.min.length = 6
content.min.confirm.length =300

# rmi port
java.rmi.metadata.hostname = 127.0.0.1
metadata.rmi.registry.port = 27313
metadata.rmi.server.name = MetaDataService

java.rmi.dedup.hostname = 127.0.0.1
dedup.rmi.registry.port = 27311
dedup.rmi.server.name = DedupService

java.rmi.monitor.hostname = 127.0.0.1
monitor.rmi.registry.port = 27312
monitor.rmi.server.name = MonitorService

# spider http listen port
http.bind.ip = 0.0.0.0
http.bind.port = 27300

# spider number
spider.num = 1

# total jobs num in this scheduler
scheduler.jobs.num = 5
# init num per time
scheduler.jobs.per.num =5